<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:Etude.ViewModels"
             xmlns:models="clr-namespace:Etude.Models"
             xmlns:behaviors="clr-namespace:Etude.Behaviors"
             x:DataType="vm:Example21ViewModel"
             x:Class="Etude.Views.Example21"
             Title="Compiled Bindings">
    <ContentPage.BindingContext>
        <vm:Example21ViewModel/>
    </ContentPage.BindingContext>
    <ListView ItemsSource="{Binding Model}">
        <ListView.Behaviors>
            <behaviors:ListViewItemTappedBehavior/>
        </ListView.Behaviors>
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="models:Band">
                <TextCell Text="{Binding Name}"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>