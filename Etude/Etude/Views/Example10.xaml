<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Etude.ViewModels"
             xmlns:behaviors="clr-namespace:Etude.Behaviors"
             mc:Ignorable="d"
             x:Class="Etude.Views.Example10"
             Title="Event to Command">
    <ContentPage.BindingContext>
        <vm:Example10ViewModel/>
    </ContentPage.BindingContext>
    <ListView x:Name="lviewFoodList"
              ItemsSource="{Binding Model}">
        <ListView.Behaviors>
            <behaviors:ListViewItemTappedBehavior/>
            <behaviors:EventToCommandBehavior EventName="ItemSelected" 
                                              Command="{Binding SelectedFoodCommand}" 
                                              Converter="{StaticResource SelectedItemEventArgsConverter}"/>
        </ListView.Behaviors>
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Name}" Detail="{Binding Description}"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>