<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:Etude.ViewModels"
             x:Class="Etude.Views.Example22"
             Title="Fody">
    <ContentPage.BindingContext>
        <vm:Example22ViewModel />
    </ContentPage.BindingContext>

    <AbsoluteLayout>
        <ScrollView AbsoluteLayout.LayoutBounds="0, 0, 1, 1" 
                    AbsoluteLayout.LayoutFlags="All">
            <StackLayout>
                <Button Text="Method Decorator" 
                    Command="{Binding Sample01Command}" />
            </StackLayout>
        </ScrollView>

        <!-- Overlay -->
        <ContentView AbsoluteLayout.LayoutBounds="0, 0, 1, 1" 
                     AbsoluteLayout.LayoutFlags="All" 
                     IsVisible="{Binding IsBusy}" 
                     Padding="10,0"
                     BackgroundColor="{StaticResource OverlayColor}">
            <ActivityIndicator IsRunning="{Binding IsBusy}" 
                               VerticalOptions="Center" />
        </ContentView>

    </AbsoluteLayout>
</ContentPage>